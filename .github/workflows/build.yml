name: 🎴 Build SwipeShot APK

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    name: Build Debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Accept Android licenses
        run: yes | sdkmanager --licenses || true

      - name: Install npm dependencies
        run: npm ci

      - name: Build web assets
        run: npm run build

      - name: Capacitor add android
        run: |
          if [ ! -d "android" ]; then
            npx cap add android
          fi

      - name: Capacitor sync
        run: npx cap sync android

      - name: Patch AndroidManifest — Android 15 permissions
        run: |
          MANIFEST="android/app/src/main/AndroidManifest.xml"
          # Remove old storage permissions if any, then inject correct ones
          sed -i '/<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE/d' "$MANIFEST"
          sed -i '/<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE/d' "$MANIFEST"
          sed -i '/<uses-permission android:name="android.permission.READ_MEDIA/d' "$MANIFEST"
          sed -i 's|</manifest>|    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>\n    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>\n    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>\n    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29"/>\n</manifest>|' "$MANIFEST"
          echo "=== AndroidManifest.xml ==="
          cat "$MANIFEST"

      - name: Patch build.gradle — target SDK 34
        run: |
          GRADLE="android/app/build.gradle"
          sed -i 's/compileSdkVersion .*/compileSdkVersion 34/' "$GRADLE"
          sed -i 's/targetSdkVersion .*/targetSdkVersion 34/' "$GRADLE"
          sed -i 's/minSdkVersion .*/minSdkVersion 26/' "$GRADLE"
          echo "=== build.gradle ==="
          cat "$GRADLE"

      - name: Build Debug APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon
        env:
          JAVA_HOME: ${{ env.JAVA_HOME }}

      - name: Rename APK
        run: |
          mkdir -p output
          cp android/app/build/outputs/apk/debug/app-debug.apk \
             output/SwipeShot-v${{ github.run_number }}.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: SwipeShot-APK-v${{ github.run_number }}
          path: output/SwipeShot-v${{ github.run_number }}.apk
          retention-days: 30

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v1.0.${{ github.run_number }}
          name: "SwipeShot v1.0.${{ github.run_number }}"
          body: |
            ## 🎴 SwipeShot APK — Android 13/14/15

            ### Instalar
            1. Descarga el `.apk`
            2. **Ajustes → Aplicaciones → Instalar apps desconocidas** ✓
            3. Abre e instala

            ### Al abrir por primera vez
            Acepta el permiso de **Fotos y videos** cuando aparezca el diálogo.
          files: output/SwipeShot-v${{ github.run_number }}.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
